[build]
builder = "nixpacks"

[deploy]
startCommand = "/app/.venv/bin/gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"