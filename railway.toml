[build]
builder = "nixpacks"

[deploy]
startCommand = "/app/.venv/bin/gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --timeout 120 --workers 2"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"